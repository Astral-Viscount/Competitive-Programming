# https://train.nzoi.org.nz/problems/1113

bar, outpost, inn = map(int, input.split())
tempest = int(input())

# keep multiplying
# check if num is bigger or smaller than tempest
# check if the next multiplicateion value subtracted from th tempest is bigger or smaller and vice versa
#if its smaller then go back and check else return the value
